[Operator specifiation for ts_argmin]
1. Type signature
ts_argmin: Signal<Float> (signal: Signal<Float>, period: Int)
2. Arguments
signal: Input signal.
period: Lookback window. Integer greater than 1.
3. Semantic Definition
Days from the lowest point. Normalized from 0 to 1.
4. Example
uptrend = ts_argmin(signal=low, period=252)